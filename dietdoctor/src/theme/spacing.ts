import { normalize } from '@/utils';

export const spacing = {
  xxs: normalize(4),
  xs: normalize(8),
  s: normalize(16),
  m: normalize(24),
  l: normalize(32),
  xl: normalize(48),
  xxl: normalize(56),
};
