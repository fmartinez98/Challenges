import { colors } from './colors';

export const theme = {
  dark: false,
  radius: {
    primary: 5,
  },
  colors: {
    primary: colors.alpha800,
    background: colors.white,
    card: colors.white,
    text: colors.black,
    border: colors.gray200,
    notification: colors.epsilon500,
    error: colors.epsilon500,
    errorBackground: colors.epsilon100,
  },
};
